@page "/login"

<div class="login-container">
    <div class="login-card">
        <h2>Iniciar Sesión</h2>
        <div class="login-form">
            <label for="email">Correo Electrónico:</label>
            <input id="email" type="email" placeholder="ejemplo@correo.com" />

            <label for="password">Contraseña:</label>
            <input id="password" type="password" placeholder="Contraseña" />

            <button class="login-btn">Ingresar</button>
        </div>
    </div>
</div>

@code{

}

<style>
    .login-container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f3f4f6;
        overflow: hidden; /* Esconde las formas que excedan el tamaño del contenedor */
        position: relative; /* Permite posicionar las formas en relación a este contenedor */
    }

        /* Agregamos algunas formas geométricas al fondo */
        .login-container::before,
        .login-container::after,
        .login-container > div::before,
        .login-container > div::after {
            content: "";
            position: absolute;
            background: rgba(0, 0, 0, 0.05); /* Color semitransparente para las formas */
        }

        /* Círculo grande en la esquina superior izquierda */
        .login-container::before {
            width: 300px;
            height: 300px;
            border-radius: 50%;
            top: -150px;
            left: -150px;
        }

        /* Rectángulo inclinado en la esquina inferior derecha */
        .login-container::after {
            width: 200px;
            height: 300px;
            background: rgba(100, 100, 250, 0.05); /* Un color ligeramente diferente */
            bottom: -50px;
            right: -100px;
            transform: rotate(25deg);
        }

        /* Círculo pequeño en la esquina inferior izquierda */
        .login-container > div::before {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            bottom: -50px;
            left: -50px;
        }

        /* Rectángulo inclinado en la esquina superior derecha */
        .login-container > div::after {
            width: 150px;
            height: 200px;
            background: rgba(250, 100, 100, 0.05); /* Otro color */
            top: -100px;
            right: -75px;
            transform: rotate(-30deg);
        }

    .login-card {
        background: white;
        border-radius: 5px;
        padding: 20px 30px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        width: 300px;
    }

    .login-form label {
        display: block;
        margin-bottom: 5px;
    }

    .login-form input {
        width: 100%;
        padding: 10px;
        margin: 10px 0;
        border: 1px solid #ccc;
        border-radius: 4px;
    }

    .login-btn {
        width: 100%;
        padding: 10px;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
    }

        .login-btn:hover {
            background-color: #0056b3;
        }
</style>